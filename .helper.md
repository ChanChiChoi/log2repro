# log2repro 发布指南

> 本文件记录了完整的 PyPI 发布流程，供手动操作参考。

---

## 1. 前置配置（只需配置一次）

### 方式 A：PyPI Trusted Publishing（推荐）

1. 登录 https://pypi.org/
2. 访问 Account Settings → Publishing
3. 点击 "Add a new publisher"
4. 填写：
   - PyPI Project Name: `log2repro`
   - Owner: `ChanChiChoi`
   - Repository: `log2repro`
   - Workflow: `publish.yml`
   - Environment: `pypi`

### 方式 B：API Token

1. 登录 https://pypi.org/
2. 访问 Account Settings → API tokens
3. 点击 "Add API token"
4. 填写：
   - Token name: `github-actions`
   - Scope: 选择 "Entire account" 或指定项目 `log2repro`
5. 复制生成的 token（格式：`pypi-xxxx...`）
6. 访问 https://github.com/ChanChiChoi/log2repro/settings/secrets/actions
7. 点击 "New repository secret"
8. 填写：
   - Name: `PYPI_API_TOKEN`
   - Secret: 粘贴上面复制的 token

### Test PyPI（可选）

1. 登录 https://test.pypi.org/
2. 访问 Account Settings → API tokens
3. 创建 token 并复制
4. GitHub 添加 Secret: `TEST_PYPI_API_TOKEN`

---

## 2. 版本号管理

需要手动更新两个位置的版本号：

| 文件 | 字段 |
|------|------|
| `pyproject.toml` | `version = "x.y.z"` |
| `src/log2repro/__init__.py` | `__version__ = "x.y.z"` |

版本号规则：

| 变更类型 | 版本变化 | 示例 |
|----------|----------|------|
| Bug 修复 | patch | 0.1.0 → 0.1.1 |
| 新功能 | minor | 0.1.0 → 0.2.0 |
| 破坏性变更 | major | 0.1.0 → 1.0.0 |

快速 bump：

```bash
# 替换为新版本号
NEW_VERSION="0.2.0"
sed -i "s/^version = .*/version = \"$NEW_VERSION\"/" pyproject.toml
sed -i "s/^__version__ = .*/__version__ = \"$NEW_VERSION\"/" src/log2repro/__init__.py
```

---

## 3. 手动发布流程

```bash
# Step 1: 确保在 main 分支且代码最新
git checkout main
git pull origin main

# Step 2: 运行测试
uv run pytest -v --tb=short

# Step 3: 更新版本号
NEW_VERSION="0.2.0"
sed -i "s/^version = .*/version = \"$NEW_VERSION\"/" pyproject.toml
sed -i "s/^__version__ = .*/__version__ = \"$NEW_VERSION\"/" src/log2repro/__init__.py

# Step 4: 提交并打标签
git add pyproject.toml src/log2repro/__init__.py
git commit -m "release: v$NEW_VERSION"
git tag "v$NEW_VERSION"

# Step 5: 推送
git push origin main
git push origin "v$NEW_VERSION"

# Step 6: 构建包
uv build

# Step 7: 检查包（应该输出: PASSED）
uvx twine check dist/*

# Step 8: 上传到 Test PyPI（先测试）
uvx twine upload --repository testpypi dist/*
# 访问 https://test.pypi.org/project/log2repro/ 确认

# Step 9: 测试安装
pip install --index-url https://test.pypi.org/simple/ log2repro

# Step 10: 上传到正式 PyPI
uvx twine upload dist/*

# Step 11: 验证
pip install log2repro==$NEW_VERSION
log2repro version
```

---

## 4. 使用 GitHub Action 自动发布

```bash
# Step 1: 更新版本号并提交
NEW_VERSION="0.2.0"
sed -i "s/^version = .*/version = \"$NEW_VERSION\"/" pyproject.toml
sed -i "s/^__version__ = .*/__version__ = \"$NEW_VERSION\"/" src/log2repro/__init__.py
git add pyproject.toml src/log2repro/__init__.py
git commit -m "release: v$NEW_VERSION"
git tag "v$NEW_VERSION"

# Step 2: 推送（触发自动发布）
git push origin main --tags

# Step 3: 创建 GitHub Release（可选，补充 Release Notes）
gh release create "v$NEW_VERSION" \
  --title "v$NEW_VERSION" \
  --notes "Release notes here"
```

GitHub Action 会自动：

- 多版本矩阵测试（Python 3.10-3.13）
- 构建 wheel 和 sdist
- 验证包
- 上传到 PyPI

```bash
# 查看发布状态
gh run list --workflow=publish.yml

# 查看具体运行日志
gh run view <run-id>
```

---

## 5. 回滚发布（如果出问题）

> PyPI 不允许重新上传相同版本号，需要 bump 新版本修复。

1. 修复问题
2. bump patch 版本：

```bash
NEW_VERSION="0.1.1"
sed -i "s/^version = .*/version = \"$NEW_VERSION\"/" pyproject.toml
sed -i "s/^__version__ = .*/__version__ = \"$NEW_VERSION\"/" src/log2repro/__init__.py
git add pyproject.toml src/log2repro/__init__.py
git commit -m "hotfix: v$NEW_VERSION"
git tag "v$NEW_VERSION"
git push origin main --tags
```

---

## 6. 常见问题

| 问题 | 解决方案 |
|------|---------|
| twine upload 报错 403 Forbidden | 检查 API Token 是否正确，或 Token 权限是否足够 |
| 版本号已存在 | PyPI 不允许重复版本号，需要 bump 到新版本 |
| GitHub Action 未触发 | 确保 tag 格式为 `v*`（如 `v0.1.0`） |
| 构建失败 | 检查 `pyproject.toml` 配置，确保所有必填字段存在 |
| twine check 失败 | 检查 README.md 是否存在且格式正确 |
| 如何查看已发布的版本 | 访问 https://pypi.org/project/log2repro/#history |

---

## 7. 发布前检查清单

- [ ] 所有测试通过：`uv run pytest -v`
- [ ] 版本号已更新：`grep version pyproject.toml` 和 `grep __version__ src/log2repro/__init__.py`
- [ ] CHANGELOG 已更新：`docs/changelog.md`
- [ ] README 已更新（如果有新功能）
- [ ] 本地构建成功：`uv build`
- [ ] twine check 通过：`uvx twine check dist/*`
- [ ] Git 工作区干净：`git status`

---

## 8. 快速命令参考

| 操作 | 命令 |
|------|------|
| 构建 | `uv build` |
| 检查 | `uvx twine check dist/*` |
| 上传到 Test PyPI | `uvx twine upload --repository testpypi dist/*` |
| 上传到 PyPI | `uvx twine upload dist/*` |
| 创建 Release | `gh release create v0.2.0 --title "v0.2.0" --notes "Release notes"` |
| 查看 Action 状态 | `gh run list --workflow=publish.yml` |
| 版本号 bump（patch） | `sed -i 's/^version = .*/version = "0.1.1"/' pyproject.toml` |
| 本地安装测试 | `pip install dist/log2repro-0.1.0-py3-none-any.whl` |
